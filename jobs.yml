jobs:
  - name: generate-manifest
    type: manifest
    steps:
      - GET mongo-image

  - name: deploy-service
    type: ecsDeploy
    steps:
      - GET generate-manifest
      - GET myEcsCluster
